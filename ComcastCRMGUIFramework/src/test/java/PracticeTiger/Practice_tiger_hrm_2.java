package PracticeTiger;

public class Practice_tiger_hrm_2 {

				public static void main(String[] args) throws Throwable ( FileInputStream fis-new FileInputStream("/data/commendatal.properties");
				Properties p-new Properties();
				 String urlp.getProperty("url");
				p.load(fis):
				 String unp.getProperty("u"
			 String pwdp.getProperty("pwd");
				 Random new Random();
				 intn.nextInt(1);
				 FileInputStream fisi-new FileInputStream("./testdata/testscript.xlsx);
				 Workbook WorkbookFactory.create(fisi); Sheet shwb.getSheet("assignment1");
				 Row row sh.getRow(1);
				String orgname row.getCell(1).tostring()+n; string optname rov.getCell(2).toString()+
				 Heboriver driver-new ChromeDriver();
				driver manage() window().maximize();
				 driver manage().timeouts().implicitlynit(Duration.of Seconds(6));
				driver.get(url);
				driver.findElement(By.mase("username")) sendkeys (un); driver.findtlement (By.name("user password")).sendKeys(pid);
				 driver.findElement(By.id("submitfutton").ick();
				rong should be created
				drlu FindElement(By.Cinefest Urganizations")).click();
				drfer findelsent(By.casselector("ing alt="Create Organisation33.click();
				driver.findElement(By.nome("scountiare")), sendays
				ر
				Thread.sleep(3000),
				driver.findElement(By.name("shi street").semirys("kspace tekpyramid"); ariver.findElement(By.poth//naut[@type).click();
				Thread.sleep(3000)
				String argleterre findelevent (spathlete Test) getText(),
				Spates.aut.printingorgata)
				Forgata.contains(re))
				System.out.println

	}

}
